build/
out*/
grid/
probes/
node_modules/

# Rendered audio: the oracle (upstream reference renders) and the corpus
# outputs both live here rather than in /tmp, which on this machine is a tmpfs
# and fills up.  Neither is ever committed -- they are regenerated by run.mjs.
oracle/
corpus/
